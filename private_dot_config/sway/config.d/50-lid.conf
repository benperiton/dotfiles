# Turn the internal panel off when the lid is closed, re-enable on open.
# --reload re-applies on `swaymsg reload` (so current lid state is honoured);
# --locked lets it fire while a screen locker is active.
set $laptop eDP-1
bindswitch --reload --locked lid:on  output $laptop disable
bindswitch --reload --locked lid:off output $laptop enable
